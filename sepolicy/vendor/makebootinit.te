type makebootini, domain;
type makebootini_exec, exec_type, vendor_file_type, file_type;

init_daemon_domain(makebootini)

allow makebootini vfat:dir { add_name search write };
allow makebootini vfat:file { create getattr open read write };
allow makebootini window_service:service_manager find;
